const incorrectName = "The name format is incorrect type a name between 2 and 50 letters";
const incorrectMessage = "The message format is incorrect type a message between 3 and 100 letters";
const incorrectEmail = "Email format is incorrect";

const nameRequired = "name is required";
const messageRequired = "message is required";
const emailRequired = "email is required";

export {
    incorrectName,
    incorrectMessage,
    incorrectEmail,
    nameRequired,
    messageRequired,
    emailRequired
}